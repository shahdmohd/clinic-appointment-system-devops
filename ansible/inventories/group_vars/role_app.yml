ansible_user: ec2-user
ansible_ssh_private_key_file: ~/.ssh/clinic-key.pem
ansible_python_interpreter: /usr/bin/python3

aws_region: us-east-1

image_tag: "latest"

app_name: "clinic-app"
app_dir: "/opt/clinic"

host_port: 8000
container_port: 8000

DEBUG: "False"
ALLOWED_HOSTS: "*"

DATABASE_NAME: "clinic"
DATABASE_USER: "clinicadmin"
DATABASE_PORT: 5432